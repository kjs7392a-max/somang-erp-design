# 2026-07-02 작업일지 — 소망 ERP

## 주제: 지문 로그인·홈 로딩 속도 대폭 개선 + 서울 리전 이전 (소망·현대 동시 배포)

커밋: `9975c3e` (main) / 배포: somang-erp, hyundai-erp 둘 다 완료 / 버전 마커 `v2026.07.02-8`

---

## 1. 근본 원인 (가장 중요)

지문 인증·홈 로딩이 느렸던 **진짜 원인은 Vercel 함수 실행 리전이 미국**이었던 것.

- Vercel 함수: `iad1`(미국 워싱턴DC)에서 실행
- Supabase DB: `icn1`(한국 인천/서울)
- → 서버가 DB 쿼리 할 때마다 **태평양 왕복** → options 조회 하나가 warm 상태에도 **1~5초**, 편차 심함

증거(응답 헤더):
- 함수: `X-Vercel-Id: icn1::iad1::...` (요청은 서울로 들어오나 실행은 미국)
- DB: `CF-RAY: ...-ICN` (한국)

**클라이언트(브라우저, 한국)→Supabase는 빨랐다(160ms).** 느린 구간은 오직 **미국 함수 ↔ 한국 DB** 뿐이었다.

### 해결
`vercel.json`에 `"regions": ["icn1"]` — 함수를 Supabase와 같은 서울 리전에서 실행.

| | 이전(미국 함수) | 이후(서울 함수) |
|---|---|---|
| options warm | 1~5초 | **0.28~0.58초** |
| options 첫 cold | ~5초 | ~1.4초 |

함수 리전 확인: `X-Vercel-Id: icn1::icn1::...` (실행도 서울).

---

## 2. 지문 인증(생체인증) — 반드시 숙지 ⭐

### 동작 구조
1. 로그인 페이지 진입/새로고침 → 등록 기기면 **잠금화면**(BiometricLockScreen) 렌더
2. 렌더 시점에 `prefetchAuthOptions()`가 서버 options를 **미리 요청**(challenge 발급, 쿠키 저장) → 탭하면 즉시 프롬프트
3. 사용자가 **"인증하기" 탭** → `startAuthentication()` → OS 생체 프롬프트
4. 스캔 성공 → verify(서버) → 세션 토큰 + profile 반환 → `setSession` → `window.location.href=/home`

### 오늘 지문 관련 변경
- **자동 실행 제거**: 예전엔 잠금화면 뜨자마자 지문 프롬프트가 자동으로 튀어나옴 → 새로고침만 해도 떠서 혼란. 이제 **"인증하기"를 눌러야** 프롬프트가 뜬다. (`login/page.tsx`의 auto-trigger `useEffect` 제거)
- **"인증하기" 버튼 깜빡임 제거**: 지문 성공 시 `finally { setLoading(false) }`가 실행되며 홈 이동 직전 버튼이 순간 노출됐음. 성공 경로에선 loading을 유지(어차피 홈으로 이동), 실패·취소 경로에서만 `setLoading(false)`. (`useWebAuthn.ts`)
- **challenge 쿠키 경로**: `/api/auth/webauthn/authenticate`, TTL 300초, HMAC 서명. options·verify가 이 쿠키로 challenge 검증. (건드리지 말 것)

### ⚠️ 건드리면 안 되는 구조 (재발 방지)
1. `prefetchAuthOptions` (잠금화면 렌더 시 options 미리 요청)
2. 지문 성공 후 `window.location.href`로 홈 이동 (router.push 아님 — AuthContext 상태 꼬임 방지)
3. challenge 쿠키 경로/서명
4. 성공 시 loading 유지 / 실패·취소 시에만 해제
- 상세: `docs/worklog/작업지시서.md`

### 미해결: 간헐 실패(≈20%)
- 증상: 바로 인증해도 10번 중 2번쯤 "지문 인증에 실패했습니다" → 재시도하면 됨.
- DB 확인: credential 중복 없음(사용자당 1개). → 중복 가설 기각.
- 조치: verify 실패 지점마다 서버 로그(`[wa-verify] FAIL: ...`) 심어둠 **(임시, 원인 확정 후 제거)**.
- **다음 단계**: 실패 재현 시 Vercel 로그를 읽어 어느 단계(쿠키 없음/challenge 만료/서명 검증 실패/세션 발급 실패)인지 확정 → 근본 수정.

---

## 3. Face ID는? (질문 답변)

**별도 코드 불필요 — 지문과 완전히 같은 흐름.**

- WebAuthn 플랫폼 인증기는 **OS가 생체 방식을 결정**한다. iPhone(Face ID 기기)에서 `startAuthentication()`을 부르면 OS가 **자동으로 Face ID 프롬프트**를 띄운다. 안드로이드면 지문, Touch ID 맥이면 지문. 우리 코드는 동일.
- `BiometricLockScreen`의 `detectBiometricType()`는 iPhone이면 "Face ID로 로그인하세요" + Face ID 아이콘, 그 외엔 "지문으로 로그인하세요"로 **라벨·아이콘만** 바꾼다. 실제 인증 로직엔 분기 없음.
- 카운터: iOS 패스키(iCloud 동기화)는 counter가 항상 0으로 오는 경우가 많은데, verify(`@simplewebauthn`)가 0/0을 정상 허용하므로 문제없음. (DB의 counter=49 credential은 값이 증가하는 안드로이드류 인증기)
- 전제: iOS 16.4+ (그 이하 Safari는 JS 미실행 이슈 — 별도 해결됨, 메모리 `ios-todo` 참고).

즉 Face ID 기기 사용자는 "인증하기" 탭 → **Face ID로 즉시 로그인**. 추가 작업 없음.

---

## 4. 홈 화면 로딩 개선

- **홈 카드 "불러오는중" 제거**: 기안(`useMyDrafts`)·결재(`useApprovalInbox`) 카드가 프로필 로드 후 각자 Supabase를 조회하며 "불러오는중"을 띄웠음. **stale-while-revalidate 캐시**(`src/lib/local-cache.ts`) 도입 — 지난 결과를 localStorage에 저장, 재진입 시 즉시 표시 + 백그라운드 갱신. 사용자별 캐시 키.
- **공지 캐시**: `AnnouncementSection`도 동일하게 localStorage 캐시로 즉시 표시.
- **홈 shell CDN 정적 서빙**: `(main)/layout.tsx`의 `export const dynamic = "force-dynamic"` 제거 → `/home` 등이 `○ (Static)`으로 빌드돼 CDN 즉시 서빙.
- **프로필 캐시(기존 유지)**: 생체 verify 응답에 profile 포함 → sessionStorage 캐시 → 홈 진입 시 Supabase 재조회 생략.

---

## 5. 기타 UX

- **로그아웃 즉시 처리**: `signOut`이 `await supabase.auth.signOut()` 네트워크를 기다려 스피너가 돌았음. `signOut({ scope: "local" })`(네트워크 대기 없음) + `window.location.href="/login"` 하드 리다이렉트로 **즉시** 이탈. (`AuthContext.tsx`)
- **홈 테스트 버튼 제거**: "[테스트] 알림 토스트 발송" 버튼과 미사용 `push` 코드 제거. (`HomeView.tsx`)
- **버전 마커**: `src/lib/version.ts`의 `APP_VERSION`을 잠금화면 하단에 표시 — 배포가 기기에 실제 도달했는지 확인용. 배포마다 숫자 증가.

---

## 6. 배포 (소망·현대 둘 다)

- **소망·현대는 같은 코드베이스 + 같은 Supabase DB(`bnlybtvdqyfxmbrcvcnv`)** 공유. 법인 구분은 `NEXT_PUBLIC_CORP`(SM/HD)와 **별도 Vercel 프로젝트**로만.
- 현대 배포법: 이 폴더를 `hyundai-erp` 프로젝트로 `vercel link` 재연결 → `vercel --prod`(현대 자체 env 자동 주입) → 검증 → `somang-erp`로 재연결 복구.
- 결과: 소망·현대 둘 다 서울 리전·오늘 코드 적용 확인. 현대 options warm 0.48~0.56초.
- ⚠️ `vercel link`가 `.env.local`에 `VERCEL_OIDC_TOKEN` 한 줄 추가함(로컬 개발용, 무해).

---

## 7. cold start 완전 차단 — 함수 예열(warmup)

첫 접속(오래 조용한 뒤 첫 로그인) cold start ~1.4초를 없애기 위해 **외부 모니터가 5분마다 함수를 핑**해 warm 유지.

- 코드: `authenticate/route.ts`에 **GET 핸들러** 추가 — `{ok:true, warm:true}` 반환, DB 미호출. (기존 POST `{action:"ping"}`도 유지)
  - GET로 만든 이유: UptimeRobot 등 무료 모니터가 GET 방식이라 바로 연결 가능.
- 설정: **UptimeRobot(무료)** 에 HTTP(s) 모니터 2개, 5분 간격:
  - `https://somang-erp.vercel.app/api/auth/webauthn/authenticate`
  - `https://hyundai-erp.vercel.app/api/auth/webauthn/authenticate`
- 원리: Vercel 함수는 ~15분 유휴 시 잠듦 → 5분 핑이면 안 잠듦 → 200명 누구도 cold 미경험.

### 참고: "은행앱처럼" 논의
- cold start·간헐실패는 "네이티브 vs 웹"이 아니라 **서버 왕복** 때문. 네이티브로 바꿔도 서버가 그대로면 동일.
- 은행앱이 즉시인 이유 = 지문이 **기기에 저장된 토큰을 여는 로컬 자물쇠**일 뿐 서버 왕복이 없어서. (웹에서도 구현 가능한 설계 선택)
- 결정: 지금은 **핑(A안)**으로 충분. 은행앱식 "로컬 토큰 언락" 재설계는 나중에 별도로 제대로(설계→구현→양기종 테스트). Supabase 세션은 이미 기기에 persist되므로 재설계 시 그 위에 지문 게이트를 얹는 방향.

---

## 남은 일
1. **지문 간헐 실패(≈20%) 원인 규명** — 서버 로그 재현 대기 → 확정 후 임시 로그 제거.
2. `somang-ward`(병동) 프로젝트도 같은 코드면 별도 배포 필요 여부 확인.
3. 원격(git push) 미실행 — 필요 시 push.
4. (선택) 은행앱식 로컬 토큰 언락 인증 재설계 — cold start·간헐실패 원천 제거용.

---

# 2026-07-02 (2) — 부서별 일정 탭 + 공유 데이터 구조 설계 (별건)

주제: "대시erp가자" 재개 — 부서별 일정 탭 화면 분기 + 대시보드 연동 구조 설계. **설계·스펙만**(코드 변경 없음).

- 커밋 `03c9fca`(main): 스펙 `docs/superpowers/specs/2026-07-02-dept-calendar-shared-structure-design.md`
- **배경**: 대시보드(DASH ERP) 미완 → 근무표·일정 입력처 없어 모바일 일정탭이 mock만. 결정 = **"구조(뼈대)만 먼저, 나중 연동"**.
- **실 DB 검증**: profiles 부서 분포 조회 — 특수뷰 필요 부서 = **간호과·원무과 2개**뿐, 나머지 ~38개 개인. `department` 값 이미 깔끔(조리/재무관리과만 alias 필요).
- **파이프라인 확정**: 휴가 신청(모바일/대시보드)→전자결재→최종승인→**DB 트리거**가 `leaves` INSERT→Realtime로 총무 대시보드·간호 근무표에 실시간 반영. 트리거=DB레벨(모든 승인경로 커버, seam=`useApprovalAction.ts:64-80`).
- **공유 테이블 3종**: `dept_shifts`/`dept_events`/`leaves`. 개인=localStorage 유지. 근무표는 leaves overlay(이중기록 금지).
- **부서 레지스트리**: `isNurseProfile()` 휴리스틱 폐기 → `calendarVariantFor(dept)` 명시적 맵. role축(exec→진료) 유지·부서축 분리.
- **범위**: 테이블·읽기RLS 활성화, 트리거·쓰기정책·Realtime은 마이그레이션 정의만(미설치).

### 구현 완료 (2026-07-02, 브랜치 `feature/dept-calendar-shared`, subagent-driven)
계획 `docs/superpowers/plans/2026-07-02-dept-calendar-shared-structure.md`(커밋 `e3abd26`)대로 8태스크 실행.

| 태스크 | 커밋 | 내용 |
|---|---|---|
| 1 마이그레이션 | `39c39db`,`790f7ac` | `supabase/migrations/20260702_dept_calendar_shared.sql` — 테이블 3종+인덱스+읽기RLS(활성). 트리거·쓰기정책 정의만(주석). **트리거는 `drafts`가 아니라 `document_contents(draft_id,body)` join으로 정정**(drafts에 body 컬럼 없음, 실검증). |
| 2 타입 | `419c8c1` | `src/types/dept-calendar.ts` (WardShift/Leave/DeptEvent/DateRange) |
| 3 레지스트리 | `0f4b95d` | `src/lib/dept/registry.ts` `calendarVariantFor`(간호=ward_shift/원무=dept_leaves/그외=personal) |
| 4 데이터층 | `562c2cd` | `src/lib/dept/data.ts` getWardShifts/getDeptLeaves(빈 결과) |
| 5 뷰 | `ef16a19` | `WardShiftView`/`DeptLeavesView` + 빈 상태 UI |
| 6 배선 | `3b1eaf8` | `CalendarView` 부서 레지스트리 배선(exec→진료 유지, personal=근무탭 숨김), 월간=개인일정만(mock 제거) |
| 7 정리 | `e554d55` | 죽은 코드 삭제: ShiftTable/ShiftDayDetail/shift-data.ts/중복 types/calendar-data.ts(527줄↓). **MOCK_EVENTS는 home-data·ClinicScheduleView가 사용중이라 유지** |

- **리뷰**: Task 1·6 정식 리뷰어 통과. **최종 whole-branch 리뷰(opus)**: Critical 0, Important 1 = `-31` 월말경계 버그(2월 등 무효날짜→PostgREST 에러→빈 화면) → **수정 커밋 `fc3887b`**(실제 말일 `new Date(y,m,0)` 계산). 보안 핵심(부서격리 RLS·fail-closed) 정확 확인.
- **검증**: `npx tsc --noEmit` + `npm run build`(14라우트, `/calendar` Static) 통과.

### 남은 일 (이 건)
1. **DB 마이그레이션 실제 적용** — 파일만 있고 production 미적용(승인 대기, 배포성 작업).
2. **실계정 런타임 검증** — 간호과/원무과/그외/exec 로그인해 일정 탭 부서분기 확인.
3. **브랜치 병합** — `feature/dept-calendar-shared` → main.
4. **이월 minor(연동 Phase)**: getDeptLeaves 구간겹침(시작일만 필터), `leaves.source_draft_id` unique 부분인덱스(재삽입 방지, rule #9), CalendarView useEffect deps 린트.
5. (나중 연동 Phase) 트리거·쓰기정책·Realtime 활성화, 근무표 leaves overlay, 총무 배달(routing) 수신. read-all 정책 금지(부서격리 불변식).

---

## 5. 외국인 조리원 4명 로그인 복구 (BUG-005)

"가자" 신호로 보류건 재개, 이슈 A 처리.

- **원인:** 조리과 외국인 4명 auth 이메일이 비표준(`ru.cook@somang.test` 등) → 로그인 규칙 `{사번}@somang.internal`과 불일치.
- **수정:** GoTrue Admin API로 이메일→`{사번소문자}@somang.internal`, 비번→`{사번}1234` 리셋.
  - RU001 이바노프 / UK001 코발렌코 / UZ001 카리모프 / ZH001 왕리
- **검증:** anon 키 signInWithPassword로 4명 전원 LOGIN OK(access_token 반환). 실동작 확인 완료.
- **주의:** `suhg@somang.com`(정체불명, 프로필 없음)은 결정 대기 → 미변경. 프로필은 정상이라 미변경.
- DB만 수정, 코드 변경 없음(배포 불필요).

---

## 6. 직원 프로필 ↔ 직원현황 엑셀 정렬 (소망·현대 전부)

원본: `직원현황_사번정리.xlsx` (SM 137 + HD 69 = 206명). "이 파일대로 프로필 맞춰라" 지시.

### 대조 결과
- 엑셀 206명 **전원 이미 DB에 정확한 사번으로 존재, 이름 100% 일치** → 신규 생성 0.
- 차이 4종류: 부서 34 / 직위 3 / 조리원 lang / 파일에 없는 계정.

### 적용한 것 (17건, 전부 비파괴)
- **외국인 조리원 lang 13명** ko→국적별: ru(SM-0120/0124/0126, HD-0020/0022), zh(SM-0123/0127/0128/0130/0131, HD-0019), uz(SM-0132), uk(HD-0021). ※진짜 조리원은 로그인·사번 정상이나 lang만 ko로 잘못돼 있던 것 (BUG-006).
- **직위 3건**: SM-0001 이사장→대표이사(+부서 이사장실→대표이사), SM-0016 총무과장→과장, SM-0029 간호사→사원.
- **SM-0129 정영복**: 비고=퇴사 → employment_status 퇴직.
- 검증: 재대조 결과 이름 0 / 직위 0 잔여, 부서 잔여 33건은 전부 의도적으로 유지한 HD 간호부뿐.

### 일부러 안 한 것
- **HD 간호부 33건 (엑셀=간호부, DB=간호과/CMS)**: 앱 코드가 `department === "간호과"`를 하드코딩(announcements.ts, dept/registry.ts, HomeView.tsx)해 병동 라우팅·공지권한·부서대시보드를 판정 → 파일대로 바꾸면 HD 병동 기능 회귀. 엑셀 '간호부'는 상위부문 라벨(SM 시트는 소속=간호과). **DB의 간호과가 앱 정본**이라 유지.
- **파일에 없는 계정 6개 손대지 않음(보고만)**: RU001/UK001/UZ001/ZH001(=아까 이슈A에서 고친 유령 조리계정, 실직원 아님), 3001/3002 테스트. 진짜 조리원은 SM-0120 등으로 별도 존재.

### 후속(미적용)
- 이슈A로 리셋한 RU001 4명은 유령계정 판명 → 비번잠금/삭제 여부 결정 대기.
- lang 재발감지: 영양과 조리원인데 lang=ko + 비고=외국국적인 케이스 자동 플래그 장치 검토(미착수).

### 유령계정 삭제 (7.의 후속)
지시로 유령 조리계정 4개 삭제(3001/3002 테스트는 존치): RU001/UK001/UZ001/ZH001 — profiles 4행 + auth 4계정 삭제(HTTP 200×4). 검증: 잔여 auth 0 / profiles 0 / 3001·3002 존치 2. 이슈A(BUG-005)에서 리셋했던 계정들이 유령으로 판명돼 회수. suhg@somang.com은 여전히 미결정 → 미삭제.

### suhg@somang.com 삭제
정체불명 계정 조사: 생성 2026-05-31, 로그인 이력 0, 이메일 미확인, 프로필 없음, 도메인 @somang.com(규칙과 불일치). 사용자 참조 테이블 15곳 전수확인 참조 0건 → 완전 고아. 지시로 삭제(HTTP 200), 검증 0. 보류건 종결.

---

## 7. 이슈B 인앱 브라우저 지문/설치 안내 — iOS 대응 + 병합·배포 완료

- **근본**: 카톡 인앱 WebView는 WebAuthn·PWA설치 둘 다 미지원(플랫폼 제약, 우회 불가). 카톡 안에서 지문을 되게 할 방법은 없음 → 감지 후 외부 브라우저로 유도가 정답(은행앱 표준).
- **iOS 대응 추가**: iOS는 PWA 설치가 Safari 전용이라 Chrome 안내 시 또 막힘. `getMobileOS()`로 배너·mypage alert를 iOS→Safari / 안드→Chrome 분기.
- **검증**: UA 6케이스(카톡 안드→Chrome, 카톡 아이폰→Safari, 일반 브라우저→배너없음) 전부 통과. tsc·next build 통과.
- **병합/배포**: fix/inapp-browser-biometric → main FF병합(77336c2 iOS + 74ad455 docs) 후 push. `vercel --prod`로 somang-erp + hyundai-erp 둘 다 배포(READY). 헬스체크 /login 200, / 307 양쪽 정상.
- **남은 검증**: 대표님이 실제 카톡으로 링크 열어 배너·외부열기·(Chrome/Safari에서)지문등록 최종 확인. 임시 워크어라운드 불필요(이제 배너가 안내).
