# 소망병원 ERP 전자결재 앱 — 작업 명세서

**프로젝트** 소망병원 ERP 전자결재 모바일 앱  
**작업 기간** 2026-04-09 ~ 2026-04-25  
**작업자** 김진석 / Claude Code (Anthropic)  
**총 커밋** 28개 | **변경 파일** 113개 | **추가 코드** 10,417줄  
**기술 스택** Next.js 16.2 (App Router) · React 19 · TypeScript · Tailwind CSS v4

---

## 1. 프로젝트 초기 구축 (2026-04-09 ~ 04-18)

### 1-1. 앱 라우터 구조 초기화
- Next.js 15 App Router 기반 폴더 구조 설계 및 초기화
- 라우트 구성: `/login`, `/home`, `/approval`, `/calendar`, `/draft`, `/mypage`
- 공통 레이아웃: `AppBottomNav` (하단 탭바), `AppHeader` (상단 헤더)
- 타입 시스템: `navigation.ts`, `role.ts`, `calendar.ts`

### 1-2. 로그인 페이지
- 소망병원 SVG 로고 + 브랜드 색상(`#2d5c6e`) 적용
- 사번/비밀번호 입력 폼, "아이디 저장" 체크박스
- 쿠키 기반 인증 미들웨어 (`src/middleware.ts`) — 미인증 시 `/login` 리다이렉트

---

## 2. 홈 화면 (2026-04-18 ~ 04-19)

### 2-1. 홈 페이지 리디자인
- 역할별 분기 렌더링: `staff` / `manager` / `exec` 3종
- **직원(staff)**: 내 기안 현황 카드, 잔여 연차 카드, 오늘 일정 카드
- **관리자(manager)**: 결재 대기 현황 카드 + 직원 카드 모두
- **임원(exec)**: 도넛 차트 결재 현황(`ApprovalStatusDonut`) + 오늘 일정
- Core Actions 3개 버튼: 기안하기(커스텀 SVG), 결재함, 일정
- 전사/부서별 공지사항 아코디언 섹션(`AnnouncementSection`)
- 아코디언 카드 컴포넌트(`AccordionCard`) — 접힘/펼침 애니메이션

---

## 3. 캘린더 (2026-04-18)

### 3-1. Google Calendar 스타일 전면 재설계
- 월간 그리드 뷰 — 날짜 클릭 시 말풍선 이벤트 목록 표시
- 격자선 정밀 처리 (border-r + border-gray-200)
- 전체 일정 토글 (접힘/펼침)
- 이벤트 카테고리 색상 뱃지: shift · meeting · training · event · deadline · personal
- 개인 일정 추가/삭제 (localStorage 저장)

---

## 4. 알림 시스템 (2026-04-25)

### 4-1. 알림 타입 & Context
- `src/types/notification.ts` — `NotifKind` (approval · approved · rejected · shift · notice) 정의
- `src/context/NotificationsContext.tsx` — `useReducer` 기반 전역 알림 상태
- `push()` / `markRead()` / `clearAll()` API

### 4-2. 헤드업 토스트
- `HeadsUpToast` — 화면 상단 슬라이드 인/아웃 알림 (z-index 200)
- 종류별 색상 바 (`NOTIF_KIND_META` 기반 자동 파생)
- 자동 3초 후 사라짐, 탭 하면 해당 화면 딥링크
- 버그 수정: 중첩 button 태그 → div+button (valid HTML)
- 버그 수정: 빠른 연속 푸시 시 타이머 경쟁 조건 해결

### 4-3. 알림함 UI
- `NotificationList` — 알림 목록 슬라이드 패널 (z-index 180)
- 종류별 아이콘 + 색상, 읽음/안읽음 구분, 전체 삭제
- `AppHeader` 벨 아이콘 — 미읽음 뱃지 (빨간 점)
- 버그 수정: 벨 뱃지 위치 오프셋 정밀 조정

### 4-4. 홈 테스트 버튼
- `HomeView`에 "[테스트] 알림 토스트 발송" 버튼 추가 — 데모 시연용

---

## 5. 근무표 (2026-04-25)

### 5-1. ShiftTable 컴포넌트
- `src/lib/shift-data.ts` — 근무 코드 메타(D·E·N·O·V·T), 간호과 7명 데이터
- 패턴 기반 4월 스케줄 자동 생성 + 개별 오버라이드(연차·교육)
- 내 이번 주 근무 카드 (오늘 강조 테두리)
- 전체 멤버 × 30일 그리드 (가로 스크롤)
- 근무 통계 카드 (내 4월 코드별 일수)
- 범례 뱃지
- 버그 수정: `TODAY_DAY`를 컴포넌트 함수 내부로 이동 (SSR stale date 방지)

### 5-2. ShiftDayDetail 바텀시트
- 날짜 셀 탭 시 해당 일자 전체 멤버 근무 목록 표시
- 슬라이드 업 애니메이션 (z-index 200)

---

## 6. 캘린더 추가 기능 (2026-04-25)

### 6-1. 뷰 토글 (월간 ↔ 근무표)
- `CalView` 타입 — "month" | "shift"
- localStorage 저장 (`somang_cal_view`) — 재방문 시 마지막 뷰 유지
- SSR-safe 패턴: `useState("month")` + `useEffect(() => setCalView(load()), [])`

### 6-2. 캘린더에서 휴가 신청 버튼
- 월간 뷰 하단 "🌴 휴가 신청" 버튼
- 날짜 범위 선택 후 `/draft?form=vacation&start=...&end=...`으로 이동

---

## 7. 기안 화면 프리필 (2026-04-25)

- URL 쿼리 파라미터 → 기안 폼 자동 입력
  - `?form=vacation&start=2026-04-20&end=2026-04-21`
  - `?form=proposal`, `?form=resignation`
- `DraftView` — `prefill` prop으로 폼 종류 + 날짜 수신
- `src/app/(main)/draft/page.tsx` — `useSearchParams()` + `Suspense` 래핑
- 버그 수정: `useEffect(fn, [])` prefill 초기화 → 동기 `useState` 초기값으로 교체 (props 기반 초기화 안티패턴 제거)

---

## 8. PDF 미리보기 (2026-04-25)

### 8-1. ApprovalStamp
- `src/components/pdf/ApprovalStamp.tsx` — 원형 결재 도장 SVG
- 승인: 붉은 테두리 + 이름, 반려: "반려" 텍스트 + 빨간 배경
- `rotate` prop으로 자연스러운 기울기
- 버그 수정: 반려 도장에서 `rotate` prop이 무시되던 문제 수정

### 8-2. PdfBody
- `src/components/pdf/PdfBody.tsx` — 서류 본문 테이블
- 연차 신청서 / 품의서 / 사직원 3종 분기 렌더링

### 8-3. PdfPreviewSheet
- `src/components/pdf/PdfPreviewSheet.tsx` — 전체 화면 PDF 미리보기 오버레이 (z-index 250)
- 황색 종이 질감 (`linear-gradient`) + Noto Serif KR 폰트
- 소망병원 워터마크
- 결재 박스 (기안자 + 결재선 도장 포함)
- 하단: 승인 완료 / 반려 상태 텍스트
- 다운로드 버튼 (2.5초 후 원복), 공유 버튼
- 버그 수정: `setTimeout` 누수 → `useRef` + `useEffect` cleanup

### 8-4. ApprovalDetailView 연동
- "PDF (도장)" 버튼 → PdfPreviewSheet 오픈
- 결재 상태(`pending` / `approved` / `rejected`)에 따라 도장 유무 분기

---

## 9. 실제 조직 데이터 반영 (2026-04-25)

**참조 파일**
- `음성소망의료재단 인력현황.xlsx` — 간호과 실제 인력
- `소망병원 서류 결제라인.xlsx` — 서류 종류별 결재선

**반영 내용**

| 항목 | 이전 (mock) | 이후 (실제) |
|---|---|---|
| 로그인 사용자 | 박지영 | 윤민주 |
| 부서 | 외과 2병동 | 간호과 |
| 근무표 인원 | 임의 이름 7명 | 윤민주·김미현·함수정·사은경·최중선·임보람·신채영 |
| 연차신청서 결재선 | 이미선·최민호·김소망 (3단계) | 한기석 총무과장 → 이강표 이사장 (2단계) |
| 기관명 | 의료법인 소망의료재단 / 소망요양병원 | 음성소망의료재단 / 소망병원 |

---

## 향후 예정 작업

| 단계 | 내용 |
|---|---|
| 2단계 | Supabase Auth + profiles 테이블 (실제 로그인) |
| 3단계 | 결재/기안 워크플로우 DB 연동 |
| 4단계 | 공지사항 + 알림 DB 연동 |
| 5단계 | 근무표 관리자 입력 기능 |
